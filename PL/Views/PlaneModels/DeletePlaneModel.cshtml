@model PL.Models.PlaneModelViewModel

@{
    ViewData["Title"] = "DeletePlaneModel";
}

<div class="container">
    <h3>Are you sure you want to delete this plane model?</h3>
    <div>
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Name)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Name)
            </dd>
            <dt class="col-sm-2">
                Production Year
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.ProductionYear)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Description)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Description)
            </dd>
        </dl>

        <form asp-action="Delete" class="btn-group">
            <input type="submit" value="Delete" class="btn btn-danger" />
            <a class="btn btn-outline-dark" asp-action="Index">Back to List</a>
        </form>
    </div>
</div>